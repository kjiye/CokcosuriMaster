import WorkingDonePresenter from './WorkingDonePresenter';
import styled from 'styled-components/native';

function WorkingDoneContainer(): JSX.Element {
  return <WorkingDonePresenter />;
}

export default WorkingDoneContainer;
